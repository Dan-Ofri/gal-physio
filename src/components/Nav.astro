---
import { SITE } from '@data/site';

const links = [
  { href: '#about',        label: 'אודות' },
  { href: '#services',     label: 'שירותים' },
  { href: '#testimonials', label: 'המלצות' },
  { href: '#contact',      label: 'יצירת קשר' },
];
---

<header
  id="site-header"
  class="fixed inset-x-0 top-0 z-50 transition-all duration-500 ease-out"
  aria-label="כותרת אתר"
>
  <div class="container-prose flex items-center justify-between py-4 md:py-5">

    <!-- Logo -->
    <a
      href="/"
      aria-label={`${SITE.name} — עמוד הבית`}
      class="group shrink-0 transition-opacity hover:opacity-85"
    >
      <img
        src="/logo.svg"
        alt="גל עופרי פיזיותרפיה — לוגו"
        class="h-16 w-auto"
        width="115"
        height="64"
      />
    </a>

    <!-- Desktop Nav Links -->
    <nav aria-label="ניווט ראשי" class="hidden items-center gap-0.5 lg:flex">
      {links.map(({ href, label }) => (
        <a
          href={href}
          class="rounded-lg px-3.5 py-2 text-sm font-medium text-neutral-600 transition-all duration-200 hover:bg-neutral-100 hover:text-neutral-900 focus-visible:bg-neutral-100 focus-visible:text-neutral-900"
        >
          {label}
        </a>
      ))}
    </nav>

    <!-- Desktop Actions -->
    <div class="hidden items-center gap-4 lg:flex">
      <a
        href={`tel:${SITE.phone}`}
        aria-label={`חייג ל-${SITE.name}: ${SITE.phone}`}
        class="flex items-center gap-1.5 text-sm font-medium text-neutral-500 transition-colors hover:text-neutral-800"
      >
        <svg class="h-4 w-4 shrink-0" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
          <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07A19.5 19.5 0 0 1 4.69 12 19.79 19.79 0 0 1 1.62 3.33a2 2 0 0 1 2-1.72h3a2 2 0 0 1 2 1.72c.127.96.361 1.903.7 2.81a2 2 0 0 1-.45 2.11L7.91 9a16 16 0 0 0 6.94 6.94l.95-.95a2 2 0 0 1 2.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0 1 22 16.92z" stroke-linecap="round" stroke-linejoin="round"/>
        </svg>
        <span dir="ltr" class="tracking-wide">{SITE.phone}</span>
      </a>
      <a
        href="#contact"
        class="btn-primary py-2.5 text-sm"
        aria-label="לחצו לתיאום ייעוץ ראשוני עם גל עופרי פיזיותרפיה"
      >
        לתיאום פגישת ייעוץ
      </a>
    </div>

    <!-- Mobile Toggle Button -->
    <button
      id="nav-toggle"
      type="button"
      class="flex h-10 w-10 items-center justify-center rounded-xl text-neutral-700 transition-colors hover:bg-neutral-100 lg:hidden"
      aria-expanded="false"
      aria-controls="mobile-menu"
      aria-label="פתח תפריט ניווט"
    >
      <!-- Hamburger icon -->
      <svg id="icon-menu" class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2" aria-hidden="true">
        <path stroke-linecap="round" d="M4 6h16M4 12h16M4 18h16"/>
      </svg>
      <!-- Close icon (hidden by default) -->
      <svg id="icon-close" class="hidden h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2" aria-hidden="true">
        <path stroke-linecap="round" d="M18 6 6 18M6 6l12 12"/>
      </svg>
    </button>

  </div>
</header>

<!-- ── Mobile Menu Overlay ── -->
<div
  id="mobile-menu"
  role="dialog"
  aria-modal="true"
  aria-label="תפריט ניווט"
>
  <!-- Top bar (mirrors header) -->
  <div class="container-prose flex items-center justify-between py-4">
    <a href="/" aria-label={`${SITE.name} — עמוד הבית`}>
      <img src="/logo.svg" alt="גל עופרי פיזיותרפיה — לוגו" class="h-12 w-auto" width="86" height="48" />
    </a>
    <button
      id="nav-close-btn"
      type="button"
      class="flex h-10 w-10 items-center justify-center rounded-xl text-neutral-500 transition-colors hover:bg-neutral-100 hover:text-neutral-800"
      aria-label="סגור תפריט ניווט"
    >
      <svg class="h-6 w-6" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2" aria-hidden="true">
        <path stroke-linecap="round" d="M18 6 6 18M6 6l12 12"/>
      </svg>
    </button>
  </div>

  <!-- Nav links (centered, full height) -->
  <nav
    aria-label="תפריט ניווט נייד"
    class="flex flex-1 flex-col items-center justify-center gap-2 px-6"
  >
    {links.map(({ href, label }) => (
      <a
        href={href}
        class="w-full rounded-2xl py-4 text-center text-xl font-semibold text-neutral-700 transition-all duration-150 hover:bg-neutral-100 hover:text-teal-700 active:scale-[0.97]"
      >
        {label}
      </a>
    ))}
  </nav>

  <!-- Bottom CTA area -->
  <div class="container-prose flex flex-col gap-3 pb-10 pt-4">
    <a
      href="#contact"
      class="btn-primary justify-center py-4 text-base"
      aria-label="תיאום ייעוץ ראשוני ופגישה ראשונה עם גל עופרי"
    >
      <svg class="h-5 w-5 shrink-0" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
        <rect x="3" y="4" width="18" height="18" rx="2" ry="2" stroke-linejoin="round"/>
        <path stroke-linecap="round" d="M16 2v4M8 2v4M3 10h18"/>
      </svg>
      תיאום ייעוץ ראשוני
    </a>
    <a
      href={`tel:${SITE.phone}`}
      class="flex items-center justify-center gap-2 text-sm text-neutral-400 transition-colors hover:text-neutral-700"
      aria-label={`חייג: ${SITE.phone}`}
    >
      <svg class="h-4 w-4" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
        <path d="M22 16.92v3a2 2 0 0 1-2.18 2 19.79 19.79 0 0 1-8.63-3.07A19.5 19.5 0 0 1 4.69 12 19.79 19.79 0 0 1 1.62 3.33a2 2 0 0 1 2-1.72h3a2 2 0 0 1 2 1.72c.127.96.361 1.903.7 2.81a2 2 0 0 1-.45 2.11L7.91 9a16 16 0 0 0 6.94 6.94l.95-.95a2 2 0 0 1 2.11-.45c.907.339 1.85.573 2.81.7A2 2 0 0 1 22 16.92z" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
      <span dir="ltr">{SITE.phone}</span>
    </a>
  </div>
</div>

<style>
  /* ── Header scroll state ── */
  #site-header {
    background: transparent;
  }
  #site-header.is-scrolled {
    background: rgb(250 248 245 / 0.94);
    backdrop-filter: blur(20px) saturate(160%);
    -webkit-backdrop-filter: blur(20px) saturate(160%);
    box-shadow: 0 1px 0 rgb(0 0 0 / 0.06), 0 4px 16px -4px rgb(0 0 0 / 0.07);
  }

  /* ── Mobile menu overlay ── */
  #mobile-menu {
    display: none;
    position: fixed;
    inset: 0;
    z-index: 40;
    flex-direction: column;
    background: rgb(250 248 245 / 0.98);
    backdrop-filter: blur(24px) saturate(160%);
    -webkit-backdrop-filter: blur(24px) saturate(160%);
  }
  #mobile-menu[data-open='true'] {
    display: flex;
  }
</style>

<script>
  const header    = document.getElementById('site-header')!;
  const toggle    = document.getElementById('nav-toggle')!;
  const closeBtn  = document.getElementById('nav-close-btn')!;
  const menu      = document.getElementById('mobile-menu')!;
  const iconMenu  = document.getElementById('icon-menu')!;
  const iconClose = document.getElementById('icon-close')!;

  // ── Scroll → frosted glass ──
  const syncHeader = () => header.classList.toggle('is-scrolled', window.scrollY > 20);
  window.addEventListener('scroll', syncHeader, { passive: true });
  syncHeader();

  // ── Menu helpers ──
  const openMenu = () => {
    menu.dataset.open = 'true';
    toggle.setAttribute('aria-expanded', 'true');
    toggle.setAttribute('aria-label', 'סגור תפריט ניווט');
    iconMenu.classList.add('hidden');
    iconClose.classList.remove('hidden');
    document.body.style.overflow = 'hidden';
    (menu.querySelector('a') as HTMLElement | null)?.focus();
  };

  const closeMenu = () => {
    menu.dataset.open = 'false';
    toggle.setAttribute('aria-expanded', 'false');
    toggle.setAttribute('aria-label', 'פתח תפריט ניווט');
    iconMenu.classList.remove('hidden');
    iconClose.classList.add('hidden');
    document.body.style.overflow = '';
    toggle.focus();
  };

  toggle.addEventListener('click', () => (menu.dataset.open === 'true' ? closeMenu() : openMenu()));
  closeBtn.addEventListener('click', closeMenu);
  menu.querySelectorAll('a').forEach((a) => a.addEventListener('click', closeMenu));
  document.addEventListener('keydown', (e) => { if (e.key === 'Escape') closeMenu(); });
</script>
